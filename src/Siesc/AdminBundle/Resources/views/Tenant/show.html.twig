    <h2>Detalle de la Organizacion</h2>

    <table class="record_properties table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>#{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Sub-domain</th>
                <td>{{ entity.subdomain }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('siesc_tenant_user_tenant', { 'id': entity.id }) }}">
            Agregar usuario 
        </a>
    </li>        
    <li>
        <a href="{{ path('siesc_tenant') }}">
            Volver al Listado
        </a>
    </li>
    <li>
        <a href="{{ path('siesc_tenant_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>
        <a href="{{ path('siesc_tenant_delete', { 'id': entity.id }) }}">
            Eliminar
        </a>
    </li>
</ul>