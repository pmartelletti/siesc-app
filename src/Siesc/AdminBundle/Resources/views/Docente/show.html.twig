    <h2>Detalle del Docente</h2>

    <table class="record_properties table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>#{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Cuil</th>
                <td>{{ entity.cuil }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Teléfono</th>
                <td>{{ entity.telefono }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ entity.direccion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('siesc_user_index') }}">
            Volver al Listado
        </a>
    </li>
    <li>
        <a href="{{ path('siesc_docente_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>
        <a href="{{ path('siesc_docente_delete', { 'id': entity.id }) }}">
            Eliminar
        </a>
    </li>
</ul>